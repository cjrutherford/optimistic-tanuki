apiVersion: apps/v1
kind: Deployment
metadata:
  name: local-hub
  namespace: optimistic-tanuki
spec:
  replicas: 2
  selector:
    matchLabels:
      app: local-hub
  template:
    metadata:
      labels:
        app: local-hub
    spec:
      containers:
        - name: local-hub
          image: cjrutherford/optimistic_tanuki_local-hub:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 4000
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "4000"
            - name: GATEWAY_URL
              value: "http://gateway:3000"
            - name: GATEWAY_WS_URL
              value: "http://gateway:3300"
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          livenessProbe:
            tcpSocket:
              port: 4000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 4000
            initialDelaySeconds: 10
            periodSeconds: 5
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: local-hub
  namespace: optimistic-tanuki
spec:
  selector:
    app: local-hub
  ports:
    - port: 8087
      targetPort: 4000
  type: LoadBalancer
