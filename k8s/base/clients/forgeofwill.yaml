apiVersion: apps/v1
kind: Deployment
metadata:
  name: forgeofwill
  namespace: optimistic-tanuki
spec:
  replicas: 2
  selector:
    matchLabels:
      app: forgeofwill
  template:
    metadata:
      labels:
        app: forgeofwill
    spec:
      containers:
        - name: forgeofwill
          image: cjrutherford/optimistic_tanuki_forgeofwill:sha-f2dbda1e9be6f29394a63a46fec51b9f82517e41
          imagePullPolicy: Always
          ports:
            - containerPort: 4000
          env:
            - name: NODE_ENV
              value: 'production'
            - name: PORT
              value: '4000'
            - name: GATEWAY_URL
              value: 'http://gateway:3000'
          resources:
            requests:
              memory: '64Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '250m'
          livenessProbe:
            tcpSocket:
              port: 4000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 4000
            initialDelaySeconds: 10
            periodSeconds: 5
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: forgeofwill
  namespace: optimistic-tanuki
spec:
  selector:
    app: forgeofwill
  ports:
    - port: 8081
      targetPort: 4000
  type: LoadBalancer
