apiVersion: apps/v1
kind: Deployment
metadata:
  name: forum
  namespace: optimistic-tanuki
spec:
  replicas: 2
  selector:
    matchLabels:
      app: forum
  template:
    metadata:
      labels:
        app: forum
    spec:
      containers:
        - name: forum
          image: cjrutherford/optimistic_tanuki_forum:sha-1d5bb482689e469f1918a464dd9837a03741beed
          imagePullPolicy: Always
          ports:
            - containerPort: 3015
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3015"
            - name: POSTGRES_HOST
              value: "postgres"
            - name: POSTGRES_PORT
              value: "5432"
            - name: REDIS_HOST
              value: "redis"
            - name: REDIS_PORT
              value: "6379"
            - name: STORAGE_STRATEGY
              value: "network"
            - name: S3_ENDPOINT
              value: "http://seaweedfs:8888"
            - name: S3_BUCKET
              value: "assets"
          envFrom:
            - secretRef:
                name: optimistic-tanuki-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            tcpSocket:
              port: 3015
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 3015
            initialDelaySeconds: 10
            periodSeconds: 5
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: forum
  namespace: optimistic-tanuki
spec:
  selector:
    app: forum
  ports:
    - port: 3015
      targetPort: 3015
  clusterIP: None
