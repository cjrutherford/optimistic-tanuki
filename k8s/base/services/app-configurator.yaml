apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-configurator
  namespace: optimistic-tanuki
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app-configurator
  template:
    metadata:
      labels:
        app: app-configurator
    spec:
      containers:
        - name: app-configurator
          image: cjrutherford/optimistic_tanuki_app-configurator:sha-1d5bb482689e469f1918a464dd9837a03741beed
          imagePullPolicy: Always
          ports:
            - containerPort: 3014
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3014"
            - name: POSTGRES_HOST
              value: "postgres"
            - name: POSTGRES_PORT
              value: "5432"
            - name: REDIS_HOST
              value: "redis"
            - name: REDIS_PORT
              value: "6379"
            - name: STORAGE_STRATEGY
              value: "network"
            - name: S3_ENDPOINT
              value: "http://seaweedfs:8888"
            - name: S3_BUCKET
              value: "assets"
          envFrom:
            - secretRef:
                name: optimistic-tanuki-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            tcpSocket:
              port: 3014
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 3014
            initialDelaySeconds: 10
            periodSeconds: 5
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: app-configurator
  namespace: optimistic-tanuki
spec:
  selector:
    app: app-configurator
  ports:
    - port: 3014
      targetPort: 3014
  clusterIP: None
