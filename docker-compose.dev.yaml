# Development Docker Compose Override
# Preferred full-stack flow:
# 1. pnpm run docker:dev
# 2. pnpm run watch:build
#
# Restart-on-change is dist-driven:
# src/ -> nx build --watch -> dist/ -> bind mount -> nodemon restart
#
# Not every service in this file currently supports restart-on-change.
# See docs/devops/docker-compose.md for the supported-service matrix.
#
services:
  authentication:
    container_name: ot_authentication # Corrected typo
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/authentication:/usr/src/app
      - /usr/src/app/node_modules # Ensures node_modules from the image are used
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app',
        '--ext',
        'js,json',
        '--exec',
        'node --inspect=0.0.0.0:9229',
        '-L',
        'main.js',
      ]
    ports:
      - '9229:9229'
    depends_on: # Added
      postgres:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M

  ot-client-interface:
    container_name: ot_client_interface # Added
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
        - GIT_SHA=${GIT_SHA:-unknown}
    volumes:
      # The new Dockerfile handles copying built assets, so direct volume mounting of dist might conflict or be unnecessary for SSR.
      # For development with hot-reloading of the SSR server, a different approach might be needed.
      # This setup assumes the Dockerfile's build process is the source of truth for the container's content.
      # If live-reloading of the Node.js server is desired, the command and volumes would need to be adjusted similar to other Node.js services.
      # For now, we remove the previous volume mounts and command that were specific to Nginx and static file serving.
      - ./apps/client-interface:/app/apps/client-interface # Mount the source code for potential rebuilds if command is adapted
      - ./libs:/app/libs # Mount libs if they are part of the build
      - ./dist/apps:/app/dist/apps # Mount the shared dist root so rebuilt app directories stay visible
      - /usr/src/app/node_modules # Keep node_modules from the image
    ports:
      - '8080:4000' # Match the new exposed port
      - '9240:9240'
    environment:
      - NODE_ENV=development
      - GIT_SHA=${GIT_SHA:-unknown}
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/client-interface/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/client-interface/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9240 /app/dist/apps/client-interface/server/server.mjs'",
      ]

  forgeofwill-client-interface:
    container_name: fow_client_interface # Added
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
        - GIT_SHA=${GIT_SHA:-unknown}
    volumes:
      - ./apps/forgeofwill:/app/apps/forgeofwill
      - ./libs:/app/libs
      - ./dist/apps:/app/dist/apps
      - /usr/src/app/node_modules # Ensures node_modules from the image are used
    ports:
      - '8081:4000'
      - '9241:9241'
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/forgeofwill/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/forgeofwill/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9241 /app/dist/apps/forgeofwill/server/server.mjs'",
      ]
    environment:
      - NODE_ENV=development

  digital-homestead-client-interface:
    container_name: dh_client_interface # Added
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
        - GIT_SHA=${GIT_SHA:-unknown}
    volumes:
      - ./apps/digital-homestead:/app/apps/digital-homestead
      - ./libs:/app/libs
      - ./dist/apps:/app/dist/apps
      - /usr/src/app/node_modules # Ensures node_modules from the image are used
    ports:
      - '8082:4000'
      - '9242:9242'
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/digital-homestead/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/digital-homestead/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9242 /app/dist/apps/digital-homestead/server/server.mjs'",
      ]
    environment:
      - NODE_ENV=development

  hai-client-interface:
    container_name: hai_client_interface
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
        - GIT_SHA=${GIT_SHA:-unknown}
    volumes:
      - ./apps/hai:/app/apps/hai
      - ./libs:/app/libs
      - ./dist/apps:/app/dist/apps
      - /usr/src/app/node_modules
    ports:
      - '8088:4000'
      - '9245:9245'
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/hai/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/hai/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9245 /app/dist/apps/hai/server/server.mjs'",
      ]
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000

  fin-commander:
    container_name: ot_fin_commander
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
        - GIT_SHA=${GIT_SHA:-unknown}
    volumes:
      - ./apps/fin-commander:/app/apps/fin-commander
      - ./libs:/app/libs
      - ./dist/apps:/app/dist/apps
      - /usr/src/app/node_modules
    ports:
      - '8089:4000'
      - '9246:9246'
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/fin-commander/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/fin-commander/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9246 /app/dist/apps/fin-commander/server/server.mjs'",
      ]
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000

  marketing-generator:
    container_name: ot_marketing_generator
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
        - GIT_SHA=${GIT_SHA:-unknown}
    volumes:
      - ./apps/marketing-generator:/app/apps/marketing-generator
      - ./libs:/app/libs
      - ./dist/apps:/app/dist/apps
      - /usr/src/app/node_modules
    ports:
      - '8092:4000'
      - '9249:9249'
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/marketing-generator/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/marketing-generator/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9249 /app/dist/apps/marketing-generator/server/server.mjs'",
      ]
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000

  ui-playground:
    container_name: ot_ui_playground
    build:
      context: .
      dockerfile: ./docker/dev/static-runtime.Dockerfile
      args:
        - GIT_SHA=${GIT_SHA:-unknown}
    volumes:
      - ./dist/apps/ui-playground/browser:/usr/share/nginx/html:ro
    ports:
      - '4310:4000'
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000

  orchestra-client:
    container_name: ot_orchestra_client
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
        - GIT_SHA=${GIT_SHA:-unknown}
    volumes:
      - ./apps/orchestra-client:/app/apps/orchestra-client
      - ./libs:/app/libs
      - ./dist/apps:/app/dist/apps
      - /usr/src/app/node_modules
    ports:
      - '8099:4000'
      - '9260:9260'
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/orchestra-client/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/orchestra-client/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9260 /app/dist/apps/orchestra-client/server/server.mjs'",
      ]
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000

  business-site:
    container_name: ot_business_site
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
        - GIT_SHA=${GIT_SHA:-unknown}
    volumes:
      - ./apps/business-site:/app/apps/business-site
      - ./libs:/app/libs
      - ./dist/apps:/app/dist/apps
      - /usr/src/app/node_modules
    ports:
      - '8094:4000'
      - '9250:9250'
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/business-site/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/business-site/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9250 /app/dist/apps/business-site/server/server.mjs'",
      ]
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000

  crdn-client-interface:
    container_name: crdn_client_interface # Added
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
    volumes:
      - ./apps/christopherrutherford-net:/app/apps/christopherrutherford-net
      - ./libs:/app/libs
      - ./dist/apps:/app/dist/apps
      - /usr/src/app/node_modules # Ensures node_modules from the image are used
    ports:
      - '8083:4000'
      - '9243:9243'
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/christopherrutherford-net/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/christopherrutherford-net/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9243 /app/dist/apps/christopherrutherford-net/server/server.mjs'",
      ]
    environment:
      - NODE_ENV=development

  owner-console:
    container_name: owner_console
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
    volumes:
      - ./apps/owner-console:/app/apps/owner-console
      - ./libs:/app/libs
      - ./dist/apps:/app/dist/apps
      - /app/node_modules
    ports:
      - '${OWNER_CONSOLE_PORT:-8084}:4000'
      - '9254:9254'
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/owner-console/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/owner-console/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9254 /app/dist/apps/owner-console/server/server.mjs'",
      ]
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
      - ADMIN_API_URL=http://admin-api:8098

  admin-api:
    container_name: ot_admin_api
    build:
      context: .
      dockerfile: ./apps/admin-api/Dockerfile
    ports:
      - '8098:8098'
    environment:
      - ADMIN_API_PORT=8098
      - ADMIN_API_DEPLOYMENT_PATH=/app/ops/deployments/production.yaml
      - ADMIN_API_SECRETS_PATH=/app/.secrets
      - ADMIN_API_WORKSPACE_ROOT=/workspace
      - PRODUCTION_IMAGE_TAG=${PRODUCTION_IMAGE_TAG:-latest}
    volumes:
      - ./ops:/app/ops
      - ./.secrets:/app/.secrets
      - ./:/workspace
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - gateway
      - authentication

  leads-app:
    container_name: ot_leads_app
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
    volumes:
      - ./apps/leads-app:/app/apps/leads-app
      - ./libs:/app/libs
      - ./dist/apps:/app/dist/apps
      - /usr/src/app/node_modules
    ports:
      - '${LEADS_APP_PORT:-8095}:4000'
      - '9244:9244'
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/leads-app/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/leads-app/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9244 /app/dist/apps/leads-app/server/server.mjs'",
      ]
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000

  system-configurator:
    container_name: ot_system_configurator
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
    volumes:
      - ./apps/system-configurator:/app/apps/system-configurator
      - ./dist/apps:/app/dist/apps
      - /usr/src/app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/system-configurator/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/system-configurator/server/server.mjs ]; do sleep 1; done; exec node /app/dist/apps/system-configurator/server/server.mjs'",
      ]
    ports:
      - '8091:4000'
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
    depends_on:
      - gateway
      - system-configurator-api

  business-configurator:
    container_name: ot_business_configurator
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
        - GIT_SHA=${GIT_SHA:-unknown}
    volumes:
      - ./apps/business-configurator:/app/apps/business-configurator
      - ./libs:/app/libs
      - ./dist/apps:/app/dist/apps
      - /usr/src/app/node_modules
    ports:
      - '${BUSINESS_CONFIGURATOR_PORT:-8096}:4000'
      - '9251:9251'
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/business-configurator/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/business-configurator/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9251 /app/dist/apps/business-configurator/server/server.mjs'",
      ]
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
    depends_on:
      - gateway

  developer-portal:
    container_name: ot_developer_portal
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
        - GIT_SHA=${GIT_SHA:-unknown}
    volumes:
      - ./apps/developer-portal:/app/apps/developer-portal
      - ./libs:/app/libs
      - ./dist/apps:/app/dist/apps
      - /usr/src/app/node_modules
    ports:
      - '${DEVELOPER_PORTAL_PORT:-8097}:4000'
      - '9252:9252'
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/developer-portal/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/developer-portal/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9252 /app/dist/apps/developer-portal/server/server.mjs'",
      ]
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
    depends_on:
      - gateway

  configurable-client:
    container_name: ot_configurable_client
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
        - GIT_SHA=${GIT_SHA:-unknown}
    volumes:
      - ./apps/configurable-client:/app/apps/configurable-client
      - ./libs:/app/libs
      - ./dist/apps:/app/dist/apps
      - /app/node_modules
    ports:
      - '${CONFIGURABLE_CLIENT_PORT:-8090}:4000'
      - '9253:9253'
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/configurable-client/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/configurable-client/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9253 /app/dist/apps/configurable-client/server/server.mjs'",
      ]
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
    depends_on:
      - gateway
      - app-configurator
      - app-configurator-seed

  store-client:
    container_name: ot_store_client
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
    volumes:
      - ./apps/store-client:/app/apps/store-client
      - ./libs:/app/libs
      - ./dist/apps:/app/dist/apps
      - /app/node_modules
    ports:
      - '${STORE_CLIENT_PORT:-8085}:4000'
      - '9255:9255'
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/store-client/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/store-client/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9255 /app/dist/apps/store-client/server/server.mjs'",
      ]
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
    depends_on:
      - gateway

  local-hub-client-interface:
    container_name: ot_local_hub
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
    volumes:
      - ./apps/local-hub:/app/apps/local-hub
      - ./libs:/app/libs
      - ./dist/apps:/app/dist/apps
      - /app/node_modules
    ports:
      - '${LOCAL_HUB_PORT:-8087}:4000'
      - '9256:9256'
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/local-hub/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/local-hub/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9256 /app/dist/apps/local-hub/server/server.mjs'",
      ]
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
    depends_on:
      - gateway

  system-configurator-api:
    container_name: ot_system_configurator_api
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/system-configurator-api:/usr/src/app
      - /usr/src/app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app',
        '--ext',
        'js,json',
        '--exec',
        'node',
        '-L',
        'main.js',
      ]
    ports:
      - '3021:3021'
    environment:
      - NODE_ENV=development
      - PORT=3021
      - DB_HOST=db
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_NAME=ot_system_configurator
      - PCPARTPICKER_SYNC_ON_START=false
    depends_on:
      postgres:
        condition: service_healthy
      db-setup:
        condition: service_completed_successfully

  app-configurator:
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/app-configurator:/usr/src/app
      - /usr/src/app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app',
        '--ext',
        'js,json',
        '--exec',
        'node',
        '-L',
        'main.js',
      ]

  app-configurator-seed:
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/app-configurator:/usr/src/app
      - /usr/src/app/node_modules
    command: sh -c "sleep 5 && node seed-script.js"

  forum:
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/forum:/usr/src/app
      - /usr/src/app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app',
        '--ext',
        'js,json',
        '--exec',
        'node',
        '-L',
        'main.js',
      ]

  permissions:
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/permissions:/usr/src/app
      - /usr/src/app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app',
        '--ext',
        'js,json',
        '--exec',
        'node',
        '-L',
        'main.js',
      ]

  store:
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/store:/usr/src/app
      - /usr/src/app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app',
        '--ext',
        'js,json',
        '--exec',
        'node',
        '-L',
        'main.js',
      ]

  chat-collector:
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/chat-collector:/usr/src/app
      - /usr/src/app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app',
        '--ext',
        'js,json',
        '--exec',
        'node',
        '-L',
        'main.js',
      ]

  prompt-proxy:
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/prompt-proxy:/usr/src/app
      - ./apps/prompt-proxy/src/assets/config.yaml:/usr/src/assets/config.yaml:ro
      - /usr/src/app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app',
        '--ext',
        'js,json',
        '--exec',
        'node',
        '-L',
        'main.js',
      ]

  ai-orchestration:
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/ai-orchestrator:/usr/src/app
      - /usr/src/app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app',
        '--ext',
        'js,json',
        '--exec',
        'node',
        '-L',
        'main.js',
      ]

  audio-workstation:
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/audio-workstation:/usr/src/app
      - /usr/src/app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app',
        '--ext',
        'js,json',
        '--exec',
        'node --inspect=0.0.0.0:9257',
        '-L',
        'main.js',
      ]
    ports:
      - '3025:3025'
      - '9257:9257'
    environment:
      - NODE_ENV=development
      - AUDIO_WORKSTATION_PORT=3025
      - DATABASE_HOST=db
      - DATABASE_PORT=5432
      - DATABASE_USER=postgres
      - DATABASE_PASSWORD=postgres
      - DATABASE_NAME=postgres
      - SERVICE_ASSETS_HOST=assets
      - SERVICE_ASSETS_PORT=3005
      - SERVICE_AI_ORCHESTRATOR_HOST=ai-orchestrator
      - SERVICE_AI_ORCHESTRATOR_PORT=3010
    depends_on:
      postgres:
        condition: service_healthy

  telos-docs-service:
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/telos-docs-service:/usr/src/app
      - /usr/src/app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app',
        '--ext',
        'js,json',
        '--exec',
        'node',
        '-L',
        'main.js',
      ]

  blogging:
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/blogging:/usr/src/app
      - /usr/src/app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app',
        '--ext',
        'js,json',
        '--exec',
        'node',
        '-L',
        'main.js',
      ]

  gateway:
    container_name: ot_gateway # Added
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps:/usr/src/app/dist/apps
      - ./apps/gateway/src/assets/config.yaml:/usr/src/app/dist/apps/gateway/assets/config.yaml:ro
      - ./libs/app-registry/src/lib/default-registry.json:/usr/src/app/config/app-registry.json:ro
      - ./scripts/wait-for-node-entrypoint.sh:/usr/src/app/scripts/wait-for-node-entrypoint.sh:ro
      - /usr/src/app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--legacy-watch',
        '--watch',
        '/usr/src/app/dist/apps/gateway',
        '--ext',
        'js,json',
        '--exec',
        'sh /usr/src/app/scripts/wait-for-node-entrypoint.sh /usr/src/app/dist/apps/gateway/main.js --inspect=0.0.0.0:9000',
      ]
    ports:
      - '3300:3300' # expose chat socket
      - '9000:9000' # Expose the debugging port
    environment:
      - NODE_ENV=development
      - APP_REGISTRY_PATH=/usr/src/app/config/app-registry.json
      - DEV_ALLOW_ALL_BROWSER_ORIGINS=true
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - GOOGLE_REDIRECT_URI=${GOOGLE_REDIRECT_URI:-}
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID:-}
      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET:-}
      - GITHUB_REDIRECT_URI=${GITHUB_REDIRECT_URI:-}
      - MICROSOFT_CLIENT_ID=${MICROSOFT_CLIENT_ID:-}
      - MICROSOFT_CLIENT_SECRET=${MICROSOFT_CLIENT_SECRET:-}
      - MICROSOFT_REDIRECT_URI=${MICROSOFT_REDIRECT_URI:-}
      - FACEBOOK_CLIENT_ID=${FACEBOOK_CLIENT_ID:-}
      - FACEBOOK_CLIENT_SECRET=${FACEBOOK_CLIENT_SECRET:-}
      - FACEBOOK_REDIRECT_URI=${FACEBOOK_REDIRECT_URI:-}
      - CLIENT_INTERFACE_DOMAIN=${CLIENT_INTERFACE_DOMAIN:-}
      - CLIENT_INTERFACE_UI_BASE_URL=${CLIENT_INTERFACE_UI_BASE_URL:-}
      - CI_GOOGLE_CLIENT_ID=${CI_GOOGLE_CLIENT_ID:-}
      - CI_GOOGLE_CLIENT_SECRET=${CI_GOOGLE_CLIENT_SECRET:-}
      - CI_GOOGLE_REDIRECT_URI=${CI_GOOGLE_REDIRECT_URI:-}
      - CI_GITHUB_CLIENT_ID=${CI_GITHUB_CLIENT_ID:-}
      - CI_GITHUB_CLIENT_SECRET=${CI_GITHUB_CLIENT_SECRET:-}
      - CI_GITHUB_REDIRECT_URI=${CI_GITHUB_REDIRECT_URI:-}
      - CI_MICROSOFT_CLIENT_ID=${CI_MICROSOFT_CLIENT_ID:-}
      - CI_MICROSOFT_CLIENT_SECRET=${CI_MICROSOFT_CLIENT_SECRET:-}
      - CI_MICROSOFT_REDIRECT_URI=${CI_MICROSOFT_REDIRECT_URI:-}
      - CI_FACEBOOK_CLIENT_ID=${CI_FACEBOOK_CLIENT_ID:-}
      - CI_FACEBOOK_CLIENT_SECRET=${CI_FACEBOOK_CLIENT_SECRET:-}
      - CI_FACEBOOK_REDIRECT_URI=${CI_FACEBOOK_REDIRECT_URI:-}
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M
    depends_on:
      postgres: # Added
        condition: service_healthy # Added
      authentication:
        condition: service_started
      ot-client-interface:
        condition: service_started
      forgeofwill-client-interface:
        condition: service_started
      digital-homestead-client-interface:
        condition: service_started
      hai-client-interface:
        condition: service_started
      profile:
        condition: service_started
      social:
        condition: service_started
      permissions:
        condition: service_started
      project-planning:
        condition: service_started
      chat-collector:
        condition: service_started
      assets:
        condition: service_started
      prompt-proxy:
        condition: service_started
      ai-orchestration:
        condition: service_started
      telos-docs-service:
        condition: service_started
      blogging:
        condition: service_started
      wellness:
        condition: service_started
      classifieds:
        condition: service_started
      payments:
        condition: service_started
      finance:
        condition: service_started
      leads-app:
        condition: service_started
      lead-tracker:
        condition: service_started
      system-configurator-api:
        condition: service_started

  profile:
    container_name: ot_profile
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/profile:/usr/src/app
      - /usr/src/app/node_modules # Ensures node_modules from the image are used
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app',
        '--ext',
        'js,json',
        '--exec',
        'node --inspect=0.0.0.0:9234',
        '-L',
        'main.js',
      ]
    ports:
      - '9234:9234'
    environment:
      - NODE_ENV=development
    depends_on: # Added
      postgres:
        condition: service_healthy
      db-setup:
        condition: service_completed_successfully

  project-planning:
    container_name: ot_project_planning
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/project-planning:/usr/src/app
      - /usr/src/app/node_modules # Ensures node_modules from the image are used
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app',
        '--ext',
        'js,json',
        '--exec',
        'node --inspect=0.0.0.0:9231',
        '-L',
        'main.js',
      ]
    ports:
      - '9231:9231'
    environment:
      - NODE_ENV=development
    depends_on: # Added
      postgres:
        condition: service_healthy

  lead-tracker:
    container_name: ot_lead_tracker
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/lead-tracker:/usr/src/app/dist/apps/lead-tracker
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        'dist/apps/lead-tracker',
        '--ext',
        'js,mjs,cjs,json',
        '--delay',
        '1',
        '--exec',
        'node --inspect=0.0.0.0:9233',
        '-L',
        'dist/apps/lead-tracker/main.js',
      ]
    ports:
      - '9233:9233'
    environment:
      - POSTGRES_HOST=postgres
      - NODE_ENV=development
      - OLLAMA_HOST=192.168.50.14
      - OLLAMA_MODEL=gemma3:4b
      - LEAD_DISCOVERY_SEARCH_GOOGLE_API_KEY=${LEAD_DISCOVERY_SEARCH_GOOGLE_API_KEY:-}
      - LEAD_DISCOVERY_SEARCH_GOOGLE_CX=${LEAD_DISCOVERY_SEARCH_GOOGLE_CX:-}
      - LEAD_DISCOVERY_GOOGLE_MAPS_API_KEY=${LEAD_DISCOVERY_GOOGLE_MAPS_API_KEY:-}
      - GOOGLE_MAPS_API_KEY=${GOOGLE_MAPS_API_KEY:-}
    depends_on:
      postgres:
        condition: service_healthy

  social:
    container_name: ot_social
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/social:/usr/src/app
      - /usr/src/app/node_modules # Ensures node_modules from the image are used
    command: ['dumb-init', 'node', '--inspect=0.0.0.0:9232', 'main.js']
    ports:
      - '9232:9232'
    environment:
      - NODE_ENV=development
    depends_on: # Added
      postgres:
        condition: service_healthy

  wellness:
    container_name: ot_wellness
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
      args:
        - APP_ROOT=/app
    volumes:
      - ./dist/apps/wellness:/app/wellness
      - ./apps/wellness/src/assets/config.yaml:/app/wellness/assets/config.yaml:ro
      - /app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/wellness',
        '--ext',
        'js,json',
        '--exec',
        'node --inspect=0.0.0.0:9206',
        '-L',
        '/app/wellness/main.js',
      ]
    ports:
      - '9206:9206'
    environment:
      - NODE_ENV=development
    depends_on:
      postgres:
        condition: service_healthy

  classifieds:
    container_name: ot_classifieds
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
      args:
        - APP_ROOT=/app
    volumes:
      - ./dist/apps:/app/dist/apps
      - ./apps/classifieds/src/assets:/app/dist/apps/assets
      - /app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/classifieds',
        '--ext',
        'js,json',
        '--exec',
        'node --inspect=0.0.0.0:9217',
        '-L',
        'dist/apps/classifieds/main.js',
      ]
    ports:
      - '9217:9217'
    environment:
      - NODE_ENV=development
    depends_on:
      postgres:
        condition: service_healthy

  payments:
    container_name: ot_payments
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps/payments:/usr/src/app
      - ./apps/payments/src/assets:/usr/src/app/assets
      - /usr/src/app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app',
        '--ext',
        'js,json',
        '--exec',
        'node --inspect=0.0.0.0:9208',
        '-L',
        'main.js',
      ]
    ports:
      - '9208:9208'
    environment:
      - NODE_ENV=development
    depends_on:
      postgres:
        condition: service_healthy
      db-setup:
        condition: service_completed_successfully

  finance:
    container_name: ot_finance
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps:/usr/src/app/dist/apps
      - /usr/src/app/node_modules
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app/dist/apps/finance',
        '--ext',
        'js,json',
        '--exec',
        'node --inspect=0.0.0.0:9209',
        '-L',
        'dist/apps/finance/main.js',
      ]
    ports:
      - '9209:9209'
    environment:
      - NODE_ENV=development
      - PORT=3016
    depends_on:
      postgres:
        condition: service_healthy
      db-setup:
        condition: service_completed_successfully

  assets:
    container_name: ot_assets
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    user: '0:0'
    volumes:
      - ./dist/apps/assets:/usr/src/app
      - /usr/src/app/node_modules
      - asset-data:/usr/src/app/storage
      - /mnt/valhalla/media:/media:ro
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app',
        '--ext',
        'js,json',
        '--exec',
        'node --inspect=0.0.0.0:9235',
        '-L',
        'main.js',
      ]
    ports:
      - '9235:9235'
    environment:
      - NODE_ENV=development
      - STORAGE_STRATEGY=local
      - LOCAL_STORAGE_PATH=/usr/src/app/storage
    depends_on:
      postgres:
        condition: service_healthy

  d6:
    container_name: ot_d6
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
        - GIT_SHA=${GIT_SHA:-unknown}
    volumes:
      - ./apps/d6:/app/apps/d6
      - ./libs:/app/libs
      - ./dist/apps:/app/dist/apps
      - /usr/src/app/node_modules
    ports:
      - '8086:4000'
      - '9247:9247'
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/d6/server',
        '--ext',
        'mjs,js,json',
        '--delay',
        '2',
        '--exec',
        "sh -c 'while [ ! -f /app/dist/apps/d6/server/server.mjs ]; do sleep 1; done; exec node --inspect=0.0.0.0:9247 /app/dist/apps/d6/server/server.mjs'",
      ]
    environment:
      - NODE_ENV=development

  videos:
    container_name: ot_videos
    build:
      context: .
      dockerfile: ./docker/dev/node-runtime.Dockerfile
    volumes:
      - ./dist/apps:/usr/src/app/dist/apps
      - /usr/src/app/node_modules
      - asset-data:/usr/src/app/storage
      - video-processing-data:/tmp/video-processing
      - /mnt/valhalla/media:/media:ro
    environment:
      - VIDEO_SEED_SOURCE_DIR=/media/Tv
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/usr/src/app/dist/apps/videos',
        '--ext',
        'js,json',
        '--exec',
        'node',
        '-L',
        'dist/apps/videos/main.js',
      ]

  video-transcoder-worker:
    container_name: ot_video_transcoder_worker
    build:
      context: .
      dockerfile: ./docker/dev/go-runtime.Dockerfile
    volumes:
      - ./dist/apps/video-transcoder-worker:/usr/src/app
      - asset-data:/usr/src/app/storage
      - video-processing-data:/tmp/video-processing
    environment:
      - PORT=3023
      - HOST=0.0.0.0
      - VIDEO_TRANSCODER_OUTPUT_ROOT=/tmp/video-processing

  video-client:
    build:
      context: .
      dockerfile: ./docker/dev/ssr-runtime.Dockerfile
      args:
        - APP_ROOT=/app
    volumes:
      - ./dist/apps/video-client:/app/dist/apps/video-client
    command:
      [
        'dumb-init',
        './node_modules/.bin/nodemon',
        '--watch',
        '/app/dist/apps/video-client/server',
        '--ext',
        'mjs,js,json',
        '--exec',
        'node --inspect=0.0.0.0:9248 /app/dist/apps/video-client/server/server.mjs',
      ]
    ports:
      - '8093:4000'
      - '9248:9248'
    environment:
      - NODE_ENV=development

volumes:
  asset-data:
