{
  "name": "configurable-client-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/configurable-client-e2e/src",
  "implicitDependencies": ["configurable-client"],
  "// targets": "to see all targets run: nx show project configurable-client-e2e --web",
  "targets": {
    "up": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -f e2e/docker-compose.configurable-client-e2e.yaml up -d --build"
      }
    },
    "down": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -f e2e/docker-compose.configurable-client-e2e.yaml down -v"
      }
    },
    "e2e": {
      "executor": "@nx/playwright:playwright",
      "options": {},
      "configurations": {
        "ci": {
          "env": {
            "BASE_URL": "http://localhost:8090"
          }
        }
      }
    },
    "e2e-docker": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx run configurable-client-e2e:up",
          "echo 'Stack started. Waiting for services...'",
          "sleep 45",
          "docker compose -f e2e/docker-compose.configurable-client-e2e.yaml run --rm playwright-runner pnpm exec nx run configurable-client-e2e:e2e --skip-nx-cache",
          "nx run configurable-client-e2e:down"
        ],
        "parallel": false
      }
    }
  }
}
