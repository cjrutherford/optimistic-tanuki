{
  "name": "store-client-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/store-client-e2e/src",
  "implicitDependencies": [
    "store-client"
  ],
  "// targets": "to see all targets run: nx show project store-client-e2e --web",
  "targets": {
    "up": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -f e2e/docker-compose.store-client-e2e.yaml up -d --build"
      }
    },
    "down": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -f e2e/docker-compose.store-client-e2e.yaml down -v"
      }
    },
    "e2e": {
      "executor": "@nx/playwright:playwright",
      "options": {},
      "configurations": {
        "ci": {
          "env": {
            "BASE_URL": "http://localhost:8085"
          }
        }
      }
    },
    "e2e-docker": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx run store-client-e2e:up",
          "echo 'Waiting for services to stabilize (45s)...'",
          "sleep 45",
          "nx run store-client-e2e:e2e",
          "nx run store-client-e2e:down"
        ],
        "parallel": false
      }
    }
  }
}