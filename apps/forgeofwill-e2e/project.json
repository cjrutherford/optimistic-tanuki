{
  "name": "forgeofwill-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/forgeofwill-e2e/src",
  "implicitDependencies": ["forgeofwill"],
  "// targets": "to see all targets run: nx show project forgeofwill-e2e --web",
  "targets": {
    "up": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -f e2e/docker-compose.forgeofwill-e2e.yaml up -d --build"
      }
    },
    "down": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -f e2e/docker-compose.forgeofwill-e2e.yaml down -v"
      }
    },
    "e2e": {
      "executor": "@nx/playwright:playwright",
      "options": {},
      "configurations": {
        "ci": {
          "env": {
            "BASE_URL": "http://localhost:8081"
          }
        }
      }
    },
    "e2e-docker": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx run forgeofwill-e2e:up",
          "echo 'Stack started. Waiting for services...'",
          "sleep 45",
          "docker compose -f e2e/docker-compose.forgeofwill-e2e.yaml run --rm playwright-runner npx nx run forgeofwill-e2e:e2e --skip-nx-cache",
          "nx run forgeofwill-e2e:down"
        ],
        "parallel": false
      }
    }
  }
}
