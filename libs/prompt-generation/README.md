# Prompt Generation

`prompt-generation` contains shared prompt-building logic used by AI-oriented features in the repo. Its source lives under `libs/prompt-generation/src/lib`.

## Repo Role

- reusable prompt assembly utilities
- shared backend and integration support for AI workflows

## Nx Commands

```bash
pnpm exec nx build prompt-generation
pnpm exec nx test prompt-generation
```
