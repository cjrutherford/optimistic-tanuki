// Canonical home is `@optimistic-tanuki/chat-contracts` (promoted per the
// bounded-context plan — single source, no shape duplication). This barrel
// re-exports for back-compat; new code imports from `chat-contracts`.
export * from '@optimistic-tanuki/chat-contracts';
