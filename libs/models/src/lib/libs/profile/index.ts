// Canonical home is `@optimistic-tanuki/profile-contracts` (promoted per the
// bounded-context plan — single source, no shape duplication). This barrel
// re-exports for back-compat; new code imports from `profile-contracts`.
export * from '@optimistic-tanuki/profile-contracts';
