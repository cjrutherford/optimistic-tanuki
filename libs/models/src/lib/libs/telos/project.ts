// Canonical home is `@optimistic-tanuki/telos-contracts` (promoted per the
// bounded-context plan — single source, no shape duplication). This barrel
// re-exports for back-compat; new code imports from `telos-contracts`.
export * from '@optimistic-tanuki/telos-contracts';
