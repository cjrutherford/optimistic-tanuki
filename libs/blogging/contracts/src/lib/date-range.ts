// Local twin of the `DateRange` tuple in `models/.../util/date-range`.
// The models copy stays for social's event service; both collapse into one
// home when social moves onto contracts (O17). One line, zero new deps.
export type DateRange = [Date, Date];
