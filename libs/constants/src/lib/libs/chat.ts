// Canonical home is `@optimistic-tanuki/chat-contracts` (promoted per O3 —
// single source, no shape duplication). This file re-exports for back-compat;
// new code imports from `chat-contracts`.
import { ChatCommands } from '@optimistic-tanuki/chat-contracts';

export default ChatCommands;
