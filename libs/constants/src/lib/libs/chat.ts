/**
 * Defines commands related to chat functionality.
 */
const ChatCommands = {
    /**
     * Command to post a new message.
     */
    POST_MESSAGE: 'POST_MESSAGE',
    /**
     * Command to get conversations.
     */
    GET_CONVERSATIONS: 'GET_CONVERSATIONS',
}

export default ChatCommands;
