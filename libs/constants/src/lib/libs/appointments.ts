// Canonical home is `@optimistic-tanuki/store-contracts` (promoted per O3 —
// single source, no shape duplication). This file re-exports for back-compat;
// new code imports from `store-contracts`.
export {
  AppointmentCommands,
  AvailabilityCommands,
  ResourceCommands,
} from '@optimistic-tanuki/store-contracts';
