// Canonical home is `@optimistic-tanuki/billing-contracts` (promoted per O3
// — single source, no shape duplication). This file re-exports for
// back-compat; new code imports from `billing-contracts`.
export { BillingCommands } from '@optimistic-tanuki/billing-contracts';
