/**
 * Defines commands related to asset management.
 */
export const AssetCommands = {
    /**
     * Command to create a new asset.
     */
    CREATE: 'asset.create',
    /**
     * Command to remove an asset.
     */
    REMOVE: 'asset.remove',
    /**
     * Command to retrieve asset metadata.
     */
    RETRIEVE: 'asset.retrieve',
    /**
     * Command to read asset content.
     */
    READ: 'asset.read',
}