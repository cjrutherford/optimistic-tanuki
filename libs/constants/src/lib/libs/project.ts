/**
 * Defines commands related to project management.
 */
export const ProjectCommands = {
    /** Command to create a new project. */
    CREATE: 'project.create',
    /** Command to update an existing project. */
    UPDATE: 'project.update',
    /** Command to delete a project. */
    DELETE: 'project.delete',
    /** Command to find a single project. */
    FIND_ONE: 'project.findOne',
    /** Command to remove a project. */
    REMOVE: 'project.remove',
    /** Command to find all projects. */
    FIND_ALL: 'project.findAll',
}

/**
 * Defines commands related to project journal management.
 */
export const ProjectJournalCommands = {
    /** Command to create a new project journal entry. */
    CREATE: 'projectJournal.create',
    /** Command to update an existing project journal entry. */
    UPDATE: 'projectJournal.update',
    /** Command to delete a project journal entry. */
    DELETE: 'projectJournal.delete',
    /** Command to find a single project journal entry. */
    FIND_ONE: 'projectJournal.findOne',
    /** Command to remove a project journal entry. */
    REMOVE: 'projectJournal.remove',
    /** Command to find all project journal entries. */
    FIND_ALL: 'projectJournal.findAll',
};

/**
 * Defines commands related to risk management.
 */
export const RiskCommands = {
    /** Command to create a new risk. */
    CREATE: 'risk.create',
    /** Command to update an existing risk. */
    UPDATE: 'risk.update',
    /** Command to delete a risk. */
    DELETE: 'risk.delete',
    /** Command to find a single risk. */
    FIND_ONE: 'risk.findOne',
    /** Command to remove a risk. */
    REMOVE: 'risk.remove',
    /** Command to find all risks. */
    FIND_ALL: 'risk.findAll',
}

/**
 * Defines commands related to task management.
 */
export const TaskCommands = {
    /** Command to create a new task. */
    CREATE: 'task.create',
    /** Command to update an existing task. */
    UPDATE: 'task.update',
    /** Command to delete a task. */
    DELETE: 'task.delete',
    /** Command to find a single task. */
    FIND_ONE: 'task.findOne',
    /** Command to remove a task. */
    REMOVE: 'task.remove',
    /** Command to find all tasks. */
    FIND_ALL: 'task.findAll',
};

/**
 * Defines commands related to change management.
 */
export const ChangeCommands = {
    /** Command to create a new change. */
    CREATE: 'change.create',
    /** Command to update an existing change. */
    UPDATE: 'change.update',
    /** Command to delete a change. */
    DELETE: 'change.delete',
    /** Command to find a single change. */
    FIND_ONE: 'change.findOne',
    /** Command to remove a change. */
    REMOVE: 'change.remove',
    /** Command to find all changes. */
    FIND_ALL: 'change.findAll',
};

/**
 * Defines commands related to timer management.
 */
export const TimerCommands = {
    /** Command to create a new timer. */
    CREATE: 'timer.create',
    /** Command to update an existing timer. */
    UPDATE: 'timer.update',
    /** Command to delete a timer. */
    DELETE: 'timer.delete',
    /** Command to find a single timer. */
    FIND_ONE: 'timer.findOne',
    /** Command to remove a timer. */
    REMOVE: 'timer.remove',
    /** Command to find all timers. */
    FIND_ALL: 'timer.findAll',
};