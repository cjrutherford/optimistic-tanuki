# app-registry

Shared application registry, cross-app navigation helpers, and registry-backed navigation components for HAI applications.

## Repo Role

- shared registry and navigation primitives across applications
- used when app discovery, app listing, or registry-backed navigation should stay consistent

## Nx Commands

```bash
pnpm exec nx build app-registry
pnpm exec nx test app-registry
```
