/**
 * Defines the structure for login data.
 */
export declare type LoginType = {
    /**
     * The user's email address.
     */
    email: string; 
    /**
     * The user's password.
     */
    password: string
}