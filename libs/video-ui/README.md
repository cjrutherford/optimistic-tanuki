# video-ui

`video-ui` contains reusable Angular UI for the video platform. Its source lives under `libs/video-ui/src/lib`.

## Repo Role

- video playback and display UI shared by video-oriented applications
- intended for frontend consumption, not backend services

## Nx Commands

```bash
pnpm exec nx build video-ui
pnpm exec nx test video-ui
```
