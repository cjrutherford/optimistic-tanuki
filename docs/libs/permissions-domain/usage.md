# permissions-domain Usage

Use this library when a consumer needs to define or resolve app-scope authorization defaults.

The important concepts are:

- permissions
- roles
- assignments
- mirrored permissions
- cross-scope mappings

The registry should be treated as the source of truth for default policy shape by scope.
