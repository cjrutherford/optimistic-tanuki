# fin-commander-imports Usage

Use this library when a host app needs a route-level or embedded transaction import workflow.

The workbench assumes:

- valid workspace and account context
- access to finance services for transaction creation
- provider selection through the registry abstraction
