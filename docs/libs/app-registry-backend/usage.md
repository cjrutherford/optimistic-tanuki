# app-registry-backend Usage

Use this library when a backend or admin-facing consumer needs:

- app registration contracts
- navigation-link contracts
- default seed registry data

Consumers should treat `DEFAULT_APP_REGISTRY` and `DEFAULT_NAVIGATION_LINKS` as seed or fallback data, not as a substitute for environment-specific registry configuration where dynamic behavior is required.
