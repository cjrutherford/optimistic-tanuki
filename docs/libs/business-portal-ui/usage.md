# business-portal-ui Usage

Use this library in a host application that provides:

- routing for owner and client portal paths
- auth context compatible with `business-data-access`
- backend access for business, authentication, asset, and related APIs

This library is most useful as a page and route package, not as isolated widget primitives.
