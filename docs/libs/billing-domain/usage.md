# billing-domain Usage

Use this library when the caller already has billing-contract inputs and needs:

- strict billing scope validation
- invoice preview calculation

The library is not a persistence layer and should not be used for repository or database access.
